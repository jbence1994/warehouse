if file == null
    then throw exception: "Null file."

if file.Length == 0
    then throw exception: "Empty file."

if file.Length > fileSettings.MaxBytes
    then throw exception: "Maximum file size exceeded."

if !fileSettings.IsSupportedType(file.FileName)
    then throw exception: "Invalid file type."
